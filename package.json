{
  "name": "@arcanemachine/pi-advice",
  "version": "0.1.4",
  "description": "Step back from current work to reconsider it with a configured model, then continue the same work.",
  "type": "module",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-extension",
    "pi-package",
    "advice",
    "advisor",
    "review"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ],
    "image": "https://raw.githubusercontent.com/arcanemachine/pi-advice/main/logo.jpg"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "watch": "tsc --noEmit --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root .",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\" package.json README.md AGENTS.md CHANGELOG.md",
    "format:check": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\" package.json README.md AGENTS.md CHANGELOG.md",
    "prepublishOnly": "npm run format:check && npm run typecheck && npm run test && npm run build"
  },
  "author": "Nicholas Moen",
  "license": "MIT",
  "files": [
    "src",
    "README.md",
    "CHANGELOG.md",
    "LICENSE.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arcanemachine/pi-advice.git"
  },
  "homepage": "https://github.com/arcanemachine/pi-advice#readme",
  "bugs": {
    "url": "https://github.com/arcanemachine/pi-advice/issues"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.82.1",
    "@types/node": "^25.3.3",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6"
  }
}
